using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExitSwing : MonoBehaviour
{
    void OnTriggerEnter(Collider other)
    {
    //    if (other.CompareTag("Player")){
    //         if(other.gameObject.GetComponent<SwingJump>().ropeScript.swinging)
    //             other.gameObject.GetComponent<SwingJump>().StopSwing();
    //    }
    }
}
